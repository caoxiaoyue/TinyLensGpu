# pytest.ini
[pytest]
markers =
    unit: Unit tests, testing the functionality of individual components
    integration: Integration tests, testing the interaction between multiple components
    slow: Tests with longer execution time
